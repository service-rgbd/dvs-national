{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "start:api": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "render:env-hints": "node scripts/render-env-hints.mjs",
    "deploy:portal": "node scripts/deploy-portal.mjs",
    "deploy:agents": "node scripts/deploy-agents.mjs",
    "deploy:discover": "node scripts/deploy-discover.mjs",
    "deploy:api": "node scripts/deploy-api.mjs",
    "deploy:cloudflare": "node scripts/deploy-api.mjs && node scripts/deploy-portal.mjs && node scripts/deploy-agents.mjs && node scripts/deploy-discover.mjs"
  },
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3",
    "wrangler": "^4.34.0"
  }
}
